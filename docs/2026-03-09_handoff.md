# 2026-03-09 Handoff (KR/EN)

## 1) 오늘 작업 요약 / Summary
- Vercel 배포 이슈를 추적했고, 문제를 두 갈래로 분리했습니다.
  - 런타임: `/api/health`가 `lightweight` 고정 (`server/auth` 모듈 로딩 실패 계열).
  - 배포: Vercel build 단계에서 `url in image config should be string`로 실패.
- 관리자 기사 수정 403 이슈를 로컬에서 재현했고, `x-actor-role: general` 전송 문제를 확인했습니다.
- 서버/클라이언트 role 정규화 패치 + ProtectedRoute role 동기화 패치를 반영했습니다(아래 참조).

## 2) 오늘 반영 후 푸시된 커밋 / Pushed Today
- `8dd11fa` fix(vercel): statically bootstrap full api routes in serverless handler
- `594e11b` fix(api): harden lightweight ai keyword fallback and remove auth bootstrap dependency
- `92a448e` Add empty commit to trigger Vercel deploy
- `43ebe5b` fix(api): normalize actor role headers and update vercel includeFiles pattern

## 3) 현재 미커밋 상태 (중요) / Current Uncommitted State (Important)
아래 변경은 **아직 커밋/푸시되지 않음**:
- `client/src/components/auth/ProtectedRoute.tsx`
  - 세션 사용자 role을 `profiles.role` 우선으로 동기화하도록 보강.
- `client/src/services/DBService.ts`
  - 데모 계정(`demo-*`)이 `/admin` 경로에서 요청 시 `x-actor-role: admin` 전송 보강.

추가로 아래 파일도 로컬 변경 상태:
- `package.json`
- `package-lock.json`
- `server/data/ai_draft_ops_metrics.json`
- 각종 로그/임시 파일(`dev-server*.log`, `build*.log`, `.local/`, `tmp_*` 등)

## 4) 재현/관찰된 핵심 이슈 / Observed Issues
1. Vercel Production deployment 실패
- 메시지: `The vercel.json schema validation failed ... url in image config should be string`
- 특이사항: repo의 `vercel.json`에는 `images` 블록이 없음. 프로젝트 측 설정/메타 꼬임 가능성 높음.

2. 로컬 관리자 기사 수정 403
- 요청: `PUT /api/articles/:id`
- 응답: `403 Forbidden`
- 확인: DevTools에서 `x-actor-role: general` 전송됨.

## 5) 내일 1순위 TODO (필수) / Tomorrow Priority-1 TODO (Mandatory)
1. **미커밋 변경 검토 후 커밋/푸시**
- 대상 우선:  
  - `client/src/components/auth/ProtectedRoute.tsx`
  - `client/src/services/DBService.ts`
- 권장 절차:
  - `git diff client/src/components/auth/ProtectedRoute.tsx client/src/services/DBService.ts`
  - `npm run lint`
  - `git add ...`
  - `git commit -m "fix(auth): sync role from profile and enforce admin actor header on /admin demo path"`
  - `git push origin main`

2. **로컬 관리자 수정 재검증**
- `/admin`에서 기사 수정 저장 재시도.
- Network 요청 헤더의 `x-actor-role`이 `admin`인지 확인.
- 여전히 403이면 `PUT /api/articles/:id`의 Request/Response payload 캡처 확보.

3. **Vercel 배포 실패 해소**
- 최신 커밋 기준 재배포 후 동일 schema 에러 재확인.
- 동일하면 Vercel 프로젝트 설정(특히 Open Graph/Image 관련 메타 설정) 초기화 또는 신규 프로젝트로 동일 repo 재연결 비교.

## 6) 메모 / Notes
- 오늘 대화 중 민감 키가 공유된 이력이 있어, 운영 전 키 로테이션(최소 `GEMINI_API_KEY`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`) 점검 필요.
- 현재 로컬 dev 서버는 필요 시 재시작 필요할 수 있음(포트 5000 충돌 이력).

