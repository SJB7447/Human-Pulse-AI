# 2026-03-04 Handoff (KR/EN)

## 1) 오늘 작업 요약 / Summary
- 기자 페이지의 AI 생성 흐름(초안/이미지/영상/발행)에서 다수 회귀 이슈를 점검하고 안정화했습니다.
- Stabilized multiple regressions across journalist AI flow (draft/image/video/publish).
- Supabase Security Advisor의 RLS 경고 대응을 반영하고, 재점검 절차를 정리했습니다.
- Addressed Supabase Security Advisor RLS warnings and documented rerun flow.
- 인터랙티브 심층 기사 생성 실패(502/스키마 차단/유사도 차단) 원인을 분리하고 재시도/가이드 로직을 보강했습니다.
- Improved failure handling for interactive longform generation (502/schema/similarity gates).
- 상세 모달 하단 스크롤/추천 뉴스 전환/카테고리 동기화 이슈를 수정했습니다.
- Fixed detail modal bottom-scroll and recommendation/category transition issues.

## 2) 핵심 변경 / Key Changes
### A. AI 초안 생성 안정화 (서버)
- `server/routes.ts`
  - 모드별 생성 규칙 미충족 시 `schema` 원인 기반 재시도 힌트 추가.
  - similarity 차단 시 재시도 사유/힌트(`retryIssueHints`) 강화.
  - longform 후보 데이터 보정 후 게이트 검증하도록 순서 정리.

### B. 제목/감정/해시태그 품질 개선
- `server/routes.ts`
  - 제목 최적화: 반복 템플릿 고정 대신 본문 기반 3개 제목 생성(실패 시 fallback 3종).
  - 감정 분석: 길이 기반 더미에서 본문 신호+모델 응답 기반 분석으로 교체, `SERENITY` 편향 완화.
  - SEO 해시태그: `#AI` 상시 고정 제거, 본문 기반 핵심 키워드 3개 추출 로직 적용.

### C. 기자 페이지 UX/동작 보정
- `client/src/pages/journalist.tsx`
  - 이미지 생성 입력을 JSON 노출 대신 자연어 디렉션 기반으로 단순화.
  - 생성 이미지 카드별 디렉션 편집/개별 재생성 유지.
  - 영상 생성을 1개에서 3개 카드로 확장, 카드별 프롬프트 수정/개별 재생성 지원.
  - 영상 표시 규격을 `16:9`, `5초` 기준으로 통일.

### D. 영상 생성 계약 정렬
- `client/src/services/gemini.ts`
  - `generateShortVideo`에 `count` 및 `duration/aspectRatio/videos[]` 응답 매핑 추가.
- `server/routes.ts`
  - `/api/ai/generate-video`: `count(1~3)` 지원, `duration: 5`, `aspectRatio: "16:9"` 반환.
  - `/api/ai/generate-video-script` 기본 문구를 가로형 16:9 5초 규격으로 수정.

### E. 상세 모달/추천 뉴스 동선
- `client/src/components/NewsDetailModal.tsx`
- `client/src/pages/emotion.tsx`
  - 추천 뉴스 클릭 시 상세 모달 전환 + 배경 목록 카테고리 동기화 복구.
  - 하단 구간 스크롤 시 끊김/지연/버튼 노출 랜덤 이슈 완화.
  - 전환 깜빡임 저감을 위한 상태 전이 로직 보강.

### F. 운영/안내/기타
- `client/src/lib/peripheralNudge.ts`
  - 감정 균형 넛지 테스트용 1분 기준을 운영 기준 시간으로 원복.
- 기사 말미 출처 안내:
  - 링크 의존 대신 `출처: {대표 출처명} · 이 기사는 AI로 재구성된 기사입니다.` 문구 정책 반영.

## 3) 주요 수정 파일 / Main Files Updated
- `server/routes.ts`
- `server/services/articlePrompt.ts`
- `server/storage.ts`
- `client/src/pages/journalist.tsx`
- `client/src/services/gemini.ts`
- `client/src/components/NewsDetailModal.tsx`
- `client/src/pages/emotion.tsx`
- `client/src/lib/peripheralNudge.ts`
- `client/src/services/DBService.ts`
- `client/src/pages/admin.tsx`

## 4) 검증 / Verification
- API/화면 수동 검증:
  - `/api/ai/generate-draft` 404 오인 이슈는 직접 URL 진입이 아닌 Fetch/XHR 호출 기준으로 확인 완료.
  - 초안 생성, 이미지 생성, 영상 3개 생성/개별 재생성 동작 확인.
- 정적 검증:
  - `npm run typecheck` 통과 (2026-03-04).
- 운영 점검:
  - Supabase Security Advisor에서 lint 재실행 후 RLS 관련 경고 소거 확인.

## 5) 남은 TODO / Next TODO
1. 영상 생성 백엔드 실제 모델 연동 시 `videos[]` 다건 응답 계약을 유지한 채 실영상 URL 발급으로 교체.
2. 상세 모달 스크롤 후반부 지연 재현 케이스(PC/모바일) 3종 기록 후 intersection threshold 0.15~0.35 범위 A/B 점검.
3. 추천 뉴스 조합 규칙(동일 카테고리 2 + 감정 균형 1)에 대한 회귀 테스트 케이스를 자동화.
